"""The benchmark: mutate a fixture, drive it through the loop, measure what happened.

It is a second use case built on the same entities, not a layer of its own. Nothing here
imports the `Healer` facade — the runner is handed a `HealLoop`.
"""
